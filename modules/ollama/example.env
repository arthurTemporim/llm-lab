# ===== O L L A M A  =====
# Bind dentro do container/rede docker. O Nginx acessa pelo nome 'ollama'.
OLLAMA_HOST=0.0.0.0

# Limite as origens do CORS para o Obsidian (Electron).
# Se for usar apps móveis, você pode concatenar (ex.: app://obsidian.md*,capacitor://localhost)
OLLAMA_ORIGINS=app://obsidian.md*

# Mantém o processo do modelo vivo entre chamadas (ajuste conforme sua RAM/VRAM)
OLLAMA_KEEP_ALIVE=5m

# Ajustes de performance (opcionais; comente/ajuste conforme hardware/modelos)
# OLLAMA_NUM_PARALLEL=1
# OLLAMA_NUM_THREADS=0        # 0 = auto (CPU auxiliar em pré-processo)
# OLLAMA_LLM_LIBRARY=          # (deixe vazio para padrão da imagem)
# OLLAMA_FLASH_ATTENTION=1     # só se sua build suportar; teste

# NVIDIA (em alguns setups é útil explicitar)
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility
